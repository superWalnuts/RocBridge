import { BaseManager } from "./base/base-manager";
import { InterfaceImplementation } from "./base/base-type";

export class JSIntefaceManager extends BaseManager
{

    registerInterface = (className: string, interfaceaName: string, impl:InterfaceImplementation, sync: boolean) => {

    }
}